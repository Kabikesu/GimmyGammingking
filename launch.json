{
    "configurations": [
        {
            "name": "Attach to Chrome",
            "port": 80,
            "request": "attach",
            "type": "chrome",
            "webRoot": "${workspaceFolder}"
        }

    
    ]
}